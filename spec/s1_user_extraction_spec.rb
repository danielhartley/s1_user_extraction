require "spec_helper"

describe S1UserExtraction do
  it "has a version number" do
    expect(S1UserExtraction::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
